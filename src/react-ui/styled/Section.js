import styled from 'styled-components'
import { 
   cssMargins, 
   cssPaddings,
   cssSizes,
   cssColorTheme } from './mixins'

export default styled.section`
   ${cssMargins}
   ${cssPaddings}
   ${cssSizes}
   ${cssColorTheme}
`